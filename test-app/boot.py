import supervisor

# Keep the status bar showing on display (shows IP, file name, etc.)
# supervisor.status_bar.display = True  # This is the default

# Disable console status bar if you don't want it in serial output
# supervisor.status_bar.console = False
